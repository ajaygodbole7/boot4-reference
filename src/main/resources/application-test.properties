# Test profile -- Testcontainers provides datasource via @ServiceConnection
# No datasource config needed -- @ServiceConnection auto-configures it
spring.docker.compose.enabled=false
spring.jpa.show-sql=true
